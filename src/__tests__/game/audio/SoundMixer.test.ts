import{describe,it,expect}from"vitest";import{SoundMixer}from"../../game/audio/SoundMixer";
describe("SoundMixer",()=>{it("ok",()=>expect(new SoundMixer()).toBeDefined());it("set/get",()=>{const x=new SoundMixer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new SoundMixer();x.disable();expect(x.isActive()).toBe(false);});});