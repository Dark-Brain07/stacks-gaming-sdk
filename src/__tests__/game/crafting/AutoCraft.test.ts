import{describe,it,expect}from"vitest";import{AutoCraft}from"../../game/crafting/AutoCraft";
describe("AutoCraft",()=>{it("ok",()=>expect(new AutoCraft()).toBeDefined());it("set/get",()=>{const x=new AutoCraft();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new AutoCraft();x.disable();expect(x.isActive()).toBe(false);});});