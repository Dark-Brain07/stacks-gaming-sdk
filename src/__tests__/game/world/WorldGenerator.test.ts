import{describe,it,expect}from"vitest";import{WorldGenerator}from"../../game/world/WorldGenerator";
describe("WorldGenerator",()=>{it("ok",()=>expect(new WorldGenerator()).toBeDefined());it("set/get",()=>{const x=new WorldGenerator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new WorldGenerator();x.disable();expect(x.isActive()).toBe(false);});});