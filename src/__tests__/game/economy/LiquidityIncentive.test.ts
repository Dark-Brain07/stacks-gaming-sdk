import{describe,it,expect}from"vitest";import{LiquidityIncentive}from"../../game/economy/LiquidityIncentive";
describe("LiquidityIncentive",()=>{it("ok",()=>expect(new LiquidityIncentive()).toBeDefined());it("state",()=>{const x=new LiquidityIncentive();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new LiquidityIncentive();x.disable();expect(x.isActive()).toBe(false);});});