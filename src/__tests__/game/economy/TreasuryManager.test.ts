import{describe,it,expect}from"vitest";import{TreasuryManager}from"../../game/economy/TreasuryManager";
describe("TreasuryManager",()=>{it("ok",()=>expect(new TreasuryManager()).toBeDefined());it("state",()=>{const x=new TreasuryManager();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TreasuryManager();x.disable();expect(x.isActive()).toBe(false);});});