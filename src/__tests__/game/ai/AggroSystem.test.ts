import{describe,it,expect}from"vitest";import{AggroSystem}from"../../game/ai/AggroSystem";
describe("AggroSystem",()=>{it("ok",()=>expect(new AggroSystem()).toBeDefined());it("set/get",()=>{const x=new AggroSystem();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new AggroSystem();x.disable();expect(x.isActive()).toBe(false);});});