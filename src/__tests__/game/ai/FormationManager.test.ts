import{describe,it,expect}from"vitest";import{FormationManager}from"../../game/ai/FormationManager";
describe("FormationManager",()=>{it("ok",()=>expect(new FormationManager()).toBeDefined());it("set/get",()=>{const x=new FormationManager();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new FormationManager();x.disable();expect(x.isActive()).toBe(false);});});