import{describe,it,expect}from"vitest";import{RewardDistributor}from"../../game/achievements/RewardDistributor";
describe("RewardDistributor",()=>{it("ok",()=>expect(new RewardDistributor()).toBeDefined());it("state",()=>{const x=new RewardDistributor();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new RewardDistributor();x.disable();expect(x.isActive()).toBe(false);});});