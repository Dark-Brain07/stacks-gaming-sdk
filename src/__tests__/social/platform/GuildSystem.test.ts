import{describe,it,expect}from"vitest";import{GuildSystem}from"../../social/platform/GuildSystem";
describe("GuildSystem",()=>{it("ok",()=>expect(new GuildSystem()).toBeDefined());it("state",()=>{const x=new GuildSystem();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new GuildSystem();x.disable();expect(x.isActive()).toBe(false);});});