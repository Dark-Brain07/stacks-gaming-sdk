import{describe,it,expect}from"vitest";import{BlockManager}from"../../social/platform/BlockManager";
describe("BlockManager",()=>{it("ok",()=>expect(new BlockManager()).toBeDefined());it("state",()=>{const x=new BlockManager();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new BlockManager();x.disable();expect(x.isActive()).toBe(false);});});