import{describe,it,expect}from"vitest";import{NonceFetcher}from"../../stacks/tools/NonceFetcher";
describe("NonceFetcher",()=>{it("ok",()=>expect(new NonceFetcher()).toBeDefined());it("state",()=>{const x=new NonceFetcher();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new NonceFetcher();x.disable();expect(x.isActive()).toBe(false);});});