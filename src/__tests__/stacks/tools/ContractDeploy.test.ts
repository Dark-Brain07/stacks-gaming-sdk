import{describe,it,expect}from"vitest";import{ContractDeploy}from"../../stacks/tools/ContractDeploy";
describe("ContractDeploy",()=>{it("ok",()=>expect(new ContractDeploy()).toBeDefined());it("state",()=>{const x=new ContractDeploy();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ContractDeploy();x.disable();expect(x.isActive()).toBe(false);});});