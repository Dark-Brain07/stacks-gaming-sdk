import{describe,it,expect}from"vitest";import{RoyaltyTracker}from"../../nft/engine/RoyaltyTracker";
describe("RoyaltyTracker",()=>{it("ok",()=>expect(new RoyaltyTracker()).toBeDefined());it("state",()=>{const x=new RoyaltyTracker();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new RoyaltyTracker();x.disable();expect(x.isActive()).toBe(false);});});