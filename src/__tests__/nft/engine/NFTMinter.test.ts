import{describe,it,expect}from"vitest";import{NFTMinter}from"../../nft/engine/NFTMinter";
describe("NFTMinter",()=>{it("ok",()=>expect(new NFTMinter()).toBeDefined());it("state",()=>{const x=new NFTMinter();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new NFTMinter();x.disable();expect(x.isActive()).toBe(false);});});