import{describe,it,expect}from"vitest";import{MintScheduler}from"../../nft/engine/MintScheduler";
describe("MintScheduler",()=>{it("ok",()=>expect(new MintScheduler()).toBeDefined());it("state",()=>{const x=new MintScheduler();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new MintScheduler();x.disable();expect(x.isActive()).toBe(false);});});