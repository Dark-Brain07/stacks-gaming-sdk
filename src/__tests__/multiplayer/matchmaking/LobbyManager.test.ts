import{describe,it,expect}from"vitest";import{LobbyManager}from"../../multiplayer/matchmaking/LobbyManager";
describe("LobbyManager",()=>{it("ok",()=>expect(new LobbyManager()).toBeDefined());it("state",()=>{const x=new LobbyManager();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new LobbyManager();x.disable();expect(x.isActive()).toBe(false);});});