import{describe,it,expect}from"vitest";import{PingOptimizer}from"../../multiplayer/matchmaking/PingOptimizer";
describe("PingOptimizer",()=>{it("ok",()=>expect(new PingOptimizer()).toBeDefined());it("state",()=>{const x=new PingOptimizer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new PingOptimizer();x.disable();expect(x.isActive()).toBe(false);});});